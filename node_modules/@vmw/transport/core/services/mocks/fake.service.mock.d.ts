import { AbstractAutoService } from '../../abstractions/abstract.autoservice';
import { FakeRequestObject, FakeResponseObject } from './service.objects.mock';
import { MessageArgs } from '../../../bus.api';
export declare class FakeService extends AbstractAutoService<FakeRequestObject, FakeResponseObject> {
    protected version: string;
    protected constructor();
    protected handleServiceRequest(requestObject: FakeRequestObject, requestArgs?: MessageArgs): void;
}

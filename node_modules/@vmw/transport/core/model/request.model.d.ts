import { AbstractFrame } from '../../bus/model/abstractframe.model';
import { UUID } from '../../bus/store/store.model';
export declare class APIRequest<PayloadT> extends AbstractFrame {
    headers: {
        [key: string]: any;
    };
    payload: PayloadT;
    request: string;
    constructor(request: string, payload: PayloadT, id: UUID, version: number, headers?: {
        [key: string]: any;
    });
}

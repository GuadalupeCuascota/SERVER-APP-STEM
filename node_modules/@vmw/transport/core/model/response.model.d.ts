import { AbstractFrame } from '../../bus/model/abstractframe.model';
import { UUID } from '../../bus/store/store.model';
export declare class APIResponse<PayloadT> extends AbstractFrame {
    payload: PayloadT;
    error: boolean;
    errorMessage: string;
    errorCode: number;
    constructor(payload: PayloadT, error: boolean, errorCode: number, errorMessage: string, id: UUID, version: number);
}

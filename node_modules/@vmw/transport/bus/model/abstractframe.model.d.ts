import { UUID } from '../store/store.model';
export declare abstract class AbstractFrame {
    id: UUID;
    created: number;
    version: number;
    headers?: {
        [key: string]: any;
    };
    constructor(id?: UUID, version?: number, headers?: {
        [key: string]: any;
    });
}

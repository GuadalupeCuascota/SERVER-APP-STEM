import { BusStoreApi, BusStore, StoreReadyResult } from '../../store.api';
import { StoreType, UUID } from './store.model';
import { EventBus } from '../../bus.api';
/**
 * Copyright(c) VMware Inc. 2016-2019
 */
export declare class StoreManager implements BusStoreApi {
    private bus;
    private galacticStoreSyncChannel;
    private internalStoreMap;
    constructor(bus: EventBus);
    openGalacticStore<T>(objectType: StoreType, brokerIdentity?: string): BusStore<T>;
    createStore<T>(objectType: StoreType, map?: Map<UUID, T>): BusStore<T>;
    getStore<T>(objectType: StoreType): BusStore<T>;
    getAllStores(): Array<BusStore<any>>;
    wipeAllStores(): void;
    destroyStore(objectType: StoreType): boolean;
    readyJoin(stores: StoreType[]): StoreReadyResult;
    private initGalacticStoreSyncChannel;
}
